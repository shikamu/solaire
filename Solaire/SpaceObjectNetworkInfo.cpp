#include "SpaceObjectNetworkInfo.h"
#include "LogicConstants.h"

SpaceObjectNetworkInfo::SpaceObjectNetworkInfo()
	: id(0), targetID(0), impactTime(0.0f), shieldRemaining(0.0f), ArmourRemaining(0.0f), targetType(TARGET_NONE) 
{
	
}
